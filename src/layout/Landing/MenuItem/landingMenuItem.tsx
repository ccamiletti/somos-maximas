

import { NavItemType } from 'types';
import maximas from './maximas';
import home from './home';

// ==============================|| MENU ITEMS ||============================== //

const landingMenuItems: { items: NavItemType[] } = {
  items: [home, maximas]
};

export default landingMenuItems;